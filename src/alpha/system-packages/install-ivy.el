;;; install-ivy.el -*- lexical-binding: t; -*-

(straight-use-package 'ivy)
(straight-use-package 'swiper)
(straight-use-package 'counsel)
(straight-use-package 'ivy-avy)

(require 'ivy)
(require 'swiper)
(require 'counsel)
(require 'ivy-avy)

;;; install-ivy.el ends here
